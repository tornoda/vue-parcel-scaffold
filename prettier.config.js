module.exports = {
    // 多少个字符换行
    printWidth: 120,
    // 缩进长度
    tabWidth: 4,
    // 用空格或者tab来缩进
    useTabs: false,
    // 声明语句后面是否加空格
    semi: true,
    // 单双引号切换
    singleQuote: true,
    // 对象属性使用引号策略
    quoteProps: 'as-needed',
    // 数组和对象及函数的最后一个成员不加','
    trailingComma: 'none',
    // if true { demo } else {demo}
    bracketSpacing: true,
    // if 'avoid' [x => x.length] elseif ('always') [(x) => x.length]
    arrowParens: 'avoid'
};
